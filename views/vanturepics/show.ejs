<% layout('layouts/boylerplate') %>

<div class="card mx-auto" style="width: 30rem">
  <img src="<%= posts.image %>" class="card-img-top" alt="..." />
  <div class="card-body">
    <h5 class="card-title"><%= posts.title %></h5>
    <p class="card-text"><%= posts.desctiption %></p>
    <ul class="list-group list-group-flush">
      <li class="list-group-item"><%= posts.username %></li>
    </ul>

    <div class="card-body">
      <div class="btn-group" role="group" aria-label="Basic outlined example">
        <a href="/vanturepics/edit" class="btn btn-outline-primary">Edit</a>
        <button type="button" class="btn btn-outline-primary">Delete</button>
        <a href="#" class="btn btn-outline-primary">Comments</a>
      </div>
    </div>
  </div>
</div>
